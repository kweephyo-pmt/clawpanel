import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow Hot Module Replacement (HMR) to work via your remote server IP
  allowedDevOrigins: ['72.62.253.53'],
};

export default nextConfig;
